{
  "name": "date-course-recommender-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "start": "nuxt start",
    "test": "vitest",
    "test:unit": "vitest run tests/unit",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "nuxt": "^3.8.0"
  },
  "devDependencies": {
    "vitest": "^0.34.0",
    "playwright": "^1.40.0"
  }
}
